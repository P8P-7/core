FUNCTION(prepend VARIABLE PREFIX)
    SET(NEWLIST "")
    FOREACH(VAR ${ARGN})
        LIST(APPEND NEWLIST "${PREFIX}/${VAR}")
    ENDFOREACH(VAR)
    SET(${VARIABLE} "${NEWLIST}" PARENT_SCOPE)
ENDFUNCTION(prepend)
